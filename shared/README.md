# Legacy HTML Helper Reference

This folder belongs to the old HTML prototype and is kept only for reference.

The active Flutter app uses Dart services and widgets under `lib/`; it does not load this JavaScript file.
