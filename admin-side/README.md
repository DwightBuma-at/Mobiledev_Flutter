# Legacy HTML Reference

This folder is kept only as a backup/reference for the old admin-side HTML prototype.

The active application is the Flutter app in `lib/`. Flutter does not import this folder and it is not listed as a Flutter asset in `pubspec.yaml`.
